apiVersion: v1
kind: Service
metadata:
  name: template-mcp-server
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: template-mcp-server-tls
  labels:
    app: template-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 8443
      targetPort: 8443
      protocol: TCP
      name: https
  selector:
    app: template-mcp-server
    component: mcp-server
