apiVersion: v1
kind: Service
metadata:
  name: template-mcp-server
  labels:
    app: template-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: template-mcp-server
    component: mcp-server
