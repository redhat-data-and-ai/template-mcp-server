apiVersion: v1
kind: Service
metadata:
  name: rfe-mcp-server
  labels:
    app: rfe-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: rfe-mcp-server
    component: mcp-server
