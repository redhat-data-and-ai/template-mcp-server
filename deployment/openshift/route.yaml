apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rfe-mcp-server
  labels:
    app: rfe-mcp-server
    component: mcp-server
spec:
  to:
    kind: Service
    name: rfe-mcp-server
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
